import React from 'react'

export default function Footer({setCurrentWikiId}) {
  return (
    <div className='footer' style={{alignItems:"baseline", display:"flex", textAlign:"center"}}>
    </div>
  )
}
